import Button from "../components/Button"

const Home = () => {
    return (
        <main className="fade-in">
            <Button id='btn' text='click' />
            <Button id='btn' text='click' />
            <Button id='btn' text='click' />
        </main>
    )
};

export default Home;